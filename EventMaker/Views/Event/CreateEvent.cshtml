@model CreateEventViewModel
@{
    ViewData["Title"] = "CreateEvent";
}
 <main class="mx-auto container mt-5">
    <div class="card">
      <div class="card-header text-center">
        <h3>Добавить мероприятие</h3>
      </div>
      <div class="card-body">
        <form method="post" enctype="multipart/form-data">
          <div class="d-flex">
            <div class="container w-75">
              <div class="d-flex">
                <div class="d-grid">
                    <label class="form-label text-end text-nowrap my-3">Логотип:</label>
                  <label class="form-label text-end text-nowrap my-3">Начало:</label>
                  <label class="form-label text-end text-nowrap my-3">Окончание:</label>
                  <label class="form-label text-end text-nowrap my-3">Мероприятие:</label>
                  <label class="form-label text-end text-nowrap my-3">Направление:</label>
                  <label class="form-label text-end text-nowrap my-3">Город:</label>

                </div>
                <div class="d-grid ms-sm-3 w-75">
                  
                  <input asp-for="@Model.StartDate" type="date" class="form-control w-25 my-2">
                  <input asp-for="@Model.EndDate" type="date" class="form-control w-25 my-2">
                  <input asp-for="@Model.Name" type="text"  class="form-control my-2">
                  @*<datalist id="Events">
                    <option value="День дня"></option>
                    <option value="День дня"></option>
                    <option value="День дня"></option>
                    <option value="День дня"></option>
                    <option value="День дня"></option>
                  </datalist>*@
                  <input asp-for="@Model.DirectionName" type="text" list="Directions" class="form-control my-2">
                  <datalist id="Directions">
                    @foreach(DirectionViewModel direction in Model.Directions){
                        <option value=@direction.Name></option>
                    }
                  </datalist>
                  <input asp-for="@Model.CityName" type="text" list="Cities" class="form-control my-2">
                  <datalist id="Cities">
                    @foreach(CityViewModel city in Model.Cities){
                        <option value=@city.Name></option>
                    }
                  </datalist>
                  <input type="file" class="form-control" name="LogoInput" asp-for="@Model.LogoInput">
                </div>
              </div>
            </div>
            <div class="container w-15 d-grid h-fitcontent">
              <button class="btn btn-primary my-2">CSV</button>
              <button class="btn btn-primary my-2">Доска</button>
            </div>
          </div>
                    
             
              
              <div class="w-50 mx-auto">
                <button type="submit" class="btn btn-primary w-100" >Ок</button>
              </div>
            
        </form>
      </div>
    </div>
  </main>


